{
  "name": "user-service-provider",
  "version": "1.0.0",
  "description": "Provider (User Service) — verifies Pact contracts published by consumers",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --testPathPattern=pact",
    "test:local": "PACT_SOURCE=local jest --testPathPattern=pact"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@pact-foundation/pact": "^12.5.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
